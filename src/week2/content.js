export const listExample = 'scores = [4, 7, 2]\nprint(scores)\n';
export const loopExample = 'scores = [4, 7, 2]\n\nfor currentScore in scores:\n    print(currentScore)\n';
export const totalExample = 'scores = [4, 7, 2]\ntotalScore = 0\n\nfor currentScore in scores:\n    totalScore = totalScore + currentScore\n\nprint(totalScore)\n';
export const countExample = 'scores = [4, 7, 2]\nscoreCount = 0\n\nfor currentScore in scores:\n    scoreCount = scoreCount + 1\n\nprint(scoreCount)\n';
export const combinedExample = 'scores = [5, 0, 2, 7]\nscoreCount = 0\ntotalScore = 0\n\nfor currentScore in scores:\n    scoreCount = scoreCount + 1\n    totalScore = totalScore + currentScore\n\nprint(scoreCount)\nprint(totalScore)\n';
const positiveExample = 'scores = [3, -1, 6]\n\nfor currentScore in scores:\n    if currentScore > 0:\n        print(currentScore)\n';
const functionExample = 'def showScores(scoreList):\n    for currentScore in scoreList:\n        print(currentScore)\n\nscores = [3, 6]\nshowScores(scores)\n';
const maximumExample = 'scores = [-4, -1, -6]\nmaximumScore = scores[0]\n\nfor currentScore in scores:\n    if currentScore > maximumScore:\n        maximumScore = currentScore\n\nprint(maximumScore)\n';
const card = data => ({hints:[], discussion:'What changed when you ran the program?', steps:[], ...data});
export const lesson = [
  card({id:'ready',kind:'welcome',phase:'Return & goal',time:'0–2 min',title:'One score becomes a collection',intro:'Last week you kept an answer in a variable. This week, collect scores and process them one at a time.',learn:'Today you will use lists and loops to announce scores and build a total. Work at your pace. Read an example, try a change, then explain what happened.',steps:['Choose your own saved profile.','If sharing: one coder types; one checker asks questions. Swap during each main task.','Everyone can use any extension or ask for support.'],discussion:'What did a variable help you keep last week?',starter:listExample}),
  card({id:'list',phase:'Read first · Do Now',time:'2–8 min',title:'Collect the club scores',intro:'Read for 2 minutes, then try your own list.',learn:'A list keeps related values in order. Square brackets contain the items; commas separate them. The name scores refers to the whole list. print(scores) displays that list. You can change an item or add another inside the brackets.',example:listExample,starter:listExample,check:'w2-list',goal:'Store at least four whole-number scores in scores and display the list.',steps:['Change one score in the editor.','Add a fourth score inside the brackets.','Run. Point to the changed and added items.'],discussion:'Why might a club keep several scores in one list?',hints:['Keep the square brackets and separate items with commas.','Use whole numbers without quotation marks.'],solution:'scores = [4, 8, 2, 5]\nprint(scores)\n',teacher:'The brackets collect four integer items. scores names the list. print displays [4, 8, 2, 5]. A list can contain repeated values and zero.',tests:'[4, 8, 2, 5] displays the same four items; also try [0, -1, 0, 6].',error:'Missing commas are syntax errors. Quoted numbers are text, not integers.'}),
  card({id:'focus',kind:'review',phase:'Types of learning',time:'8–10 min',title:'What do I need to get better at?'}),
  card({id:'loop',phase:'Read first · a loop',time:'10–14 min',title:'Visit one item at a time',intro:'Explore a short example before Main Task 1.',learn:'A for loop visits each item in a list. On each visit, currentScore holds that item. The indented instruction runs once for each visit, called an iteration. A colon begins the block. Use four spaces to show which instruction belongs inside it.',example:loopExample,starter:loopExample,steps:['Choose Walk through.','Use Next iteration to find each score.','Explain why three items give three iterations.'],discussion:'In iteration 2, what value does currentScore hold? Use the trace you have just seen.',hints:['currentScore holds an item value, not an iteration number.'],teacher:'For [4, 7, 2], currentScore holds 4, then 7, then 2. The indented print executes three times. The list remains unchanged.',tests:'Output: 4, 7 and 2 on separate lines.',error:'Without indentation, Python cannot identify the loop body.'}),
  card({id:'announcer',phase:'Main Task 1',time:'14–27 min',title:'Build the score announcer',intro:'Your club needs one score announced on each line.',learn:'Use the loop pattern with your own list. The loop variable receives the next item before the body runs. Changing the list changes how many times the body runs. Keep the print instruction inside the loop to display each item separately.',example:loopExample,starter:'scores = [6, 0, 3, 8]\n\n# Write a for loop to display each score.\n',check:'w2-announcer',goal:'Use a for loop to print every item in scores on its own line, in order.',steps:['Write a loop that prints each score.','Run, change the list, and run again.','Check work. Explain one iteration using its actual value.'],discussion:'What happens if you move print outside the loop? Run a small example and explain.',hints:['Start with: for currentScore in scores:','Indent print(currentScore) by four spaces.'],extension:'When the core check passes, use a separate bonusScore = currentScore + 2 inside the loop and display bonusScore. Run to test this variation; the core check still expects the original scores.',solution:'scores = [6, 0, 3, 8]\n\nfor currentScore in scores:\n    print(currentScore)\n',teacher:'The loop assigns each list item to currentScore. The indented print runs with that value. Moving the print outside displays only the last score for a non-empty list; an empty list would leave currentScore unset.',tests:'[6, 0, 3, 8] -> 6 / 0 / 3 / 8; [5, -2, 5] -> 5 / -2 / 5; [] -> no output.',error:'Printing scores inside the loop displays the entire list repeatedly.'}),
  card({id:'pitstop1',kind:'review',checkpoint:'pitstop1',phase:'Learning pit stop 1',time:'27–30 min',title:'Where am I now?'}),
  card({id:'totals',phase:'Read first · count and total',time:'30–35 min',title:'Build a result as you go',intro:'Explore the total first. Then compare it with a counter.',learn:'Start totalScore at zero before the loop. Each iteration adds currentScore to the total already collected. Python calculates the right side first, then stores the result on the left. A counter uses the same pattern but adds 1 for each item.',example:totalExample,starter:totalExample,extraExamples:[{title:'Count the items',code:countExample},{title:'Keep an input as a number',code:'answer = input("Bonus score: ")\nbonusScore = int(answer)\nprint(bonusScore)\n'}],steps:['Walk through the total example.','Find iteration 2: 4 + 7 becomes 11.','Explore the counter example. Explain what it adds.'],discussion:'A score is zero. Why does it increase the count but not the total?',teacher:'Initialise once before the loop. On each iteration, read the old total, add the current score, and replace the total. Count adds 1 regardless of the score value. The final unindented print runs once.',tests:'[4, 7, 2] -> total 13; the separate counter example outputs 3.',error:'Resetting totalScore to zero inside the loop loses earlier scores. totalScore = currentScore only keeps the latest score.'}),
  card({id:'desk',phase:'Main Task 2',time:'35–50 min',title:'Build the club score desk',intro:'Find the total. Add a counter when you are ready.',learn:'A running total starts before the loop and changes inside it. Display the final result after the loop. Use the worked example to help, then choose your own tests. Adding a counter gives a second result: how many scores were processed.',example:totalExample,starter:'scores = [6, 0, 3, 8]\n\n# Start a total before the loop.\n# Add each score inside the loop.\n# Display the final total after the loop.\n',scaffold:'scores = [6, 0, 3, 8]\ntotalScore = 0\n\nfor currentScore in scores:\n    # Replace pass with your total update.\n    pass\n\nprint(totalScore)\n',check:'w2-total',goal:'Use a for loop to calculate the total. Print just the total, or the count followed by the total.',steps:['Build and run a total for your list.','Test a different list, including zero.','Explain one update. Add a counter if ready.'],discussion:'Why is totalScore allowed on both sides of the equals sign?',hints:['Start with totalScore = 0 before the loop.','Inside the loop: totalScore = totalScore + currentScore','For an optional counter, start scoreCount at 0 and add 1 inside the loop. Print count first, then total.'],solution:combinedExample,teacher:'The right side uses the old total. For [5, 0, 2, 7], totals become 5, 5, 7, 14 and counts become 1, 2, 3, 4. The zero is still an item. Both final print lines are outside the loop.',tests:'[5, 0, 2, 7] -> 4 then 14 (or 14 for total only); [-3, 0, 8] -> 3 then 5; [] -> 0 then 0.',error:'An initial total inside the loop is reset each time. Starting a counter at 1 overcounts by one.'}),
  card({id:'pitstop2',kind:'review',checkpoint:'pitstop2',phase:'Learning pit stop 2',time:'50–54 min',title:'What does my work show?'}),
  card({id:'plenary',phase:'Plenary & save',time:'54–60 min',title:'Show it with fresh scores',intro:'Try this independently, then keep your evidence.',learn:'Counting adds 1 for every item. Totalling adds the item value. Start both before the loop and display both after it. A useful explanation names an actual old value, the added value and the new value from one iteration.',example:'# An explanation after running:\n# The old total was 5. The current score was 0.\n# 5 + 0 gives 5, so the total stayed at 5.\n# The count increased from 1 to 2.\n',starter:'scores = [5, 0, 2, 7]\n\n# Build a count and total without copying.\n',check:'w2-count-total',goal:'Use an explicit loop. Print the count first and total second, on separate lines.',steps:['Build a count and total for the fresh list.','Run and explain the zero-score iteration.','Download your PDF and lesson backup.'],discussion:'Which line can you now explain more clearly than at the start?',hints:['Initialise scoreCount and totalScore before the loop.','Count adds 1. Total adds currentScore.'],solution:combinedExample,teacher:'Expected count 4 and total 14. In iteration 2 the zero keeps totalScore at 5 while scoreCount moves from 1 to 2. Ask the student to explain without reading a model sentence first.',tests:'[5, 0, 2, 7] -> 4 then 14. The checker also uses a different and an empty list.',error:'A correct output copied as print(14) does not demonstrate a general method.'}),
];
export const extensions = [
  card({id:'prebronze2',level:'Pre-Bronze',colour:'mint',phase:'Open extension',time:'Your pace',title:'Small-list score keeper',intro:'A supported way to practise a running total.',learn:'Use a short list so you can follow every addition. Keep the initial total outside the loop. Add each item to the previous total and print the result after the loop. Ask for the missing-line starter if that helps.',example:totalExample,starter:'scores = [2, 5, 1]\ntotalScore = 0\n\nfor currentScore in scores:\n    # Replace pass with the total update.\n    pass\n\nprint(totalScore)\n',check:'w2-prebronze',goal:'Use a for loop and print the total of scores.',steps:['Complete the update line.','Run and test another short list.','Keep one trace step and explain it.'],hints:['Add the current score to the total already collected.'],solution:'scores = [2, 5, 1]\ntotalScore = 0\n\nfor currentScore in scores:\n    totalScore = totalScore + currentScore\n\nprint(totalScore)\n',teacher:'Totals after each iteration: 2, 7, 8. In iteration 2, 2 + 5 = 7. The starting zero is not an extra item.',tests:'[2, 5, 1] -> 8; [0, 4] -> 4; [] -> 0.',error:'Writing totalScore + currentScore without assignment discards the calculated value.'}),
  card({id:'bronze2',level:'Bronze',colour:'peach',phase:'Open extension',time:'Your pace',title:'Count every score',intro:'Combine two updates in one loop.',learn:'Use one variable to count items and another to collect their total. A zero or negative value is still an item. Count increases by 1; total changes by currentScore. Print the count first, then the total on the next line.',example:countExample,starter:'scores = [4, 0, -2, 6]\n\n# Build count and total with one for loop.\n',check:'w2-count-total',goal:'Use a loop. Print count, then total on separate lines.',steps:['Build both results in one loop.','Test zero and negative scores.','Explain why your two updates are different.'],hints:['Start both variables at zero before the loop.','Use two indented assignment statements.'],solution:combinedExample.replace('[5, 0, 2, 7]','[4, 0, -2, 6]'),teacher:'The four items have total 8. Counts progress 1, 2, 3, 4; totals progress 4, 4, 2, 8. Negative scores reduce the total but still increase the count.',tests:'[4, 0, -2, 6] -> 4 then 8; [] -> 0 then 0.',error:'Adding currentScore to scoreCount calculates another total, not the item count.'}),
  card({id:'silver2',level:'Silver',colour:'blue',phase:'Open extension',time:'Your pace',title:'Keep only positive scores',intro:'Use a condition inside your loop, then reuse the method in a function.',learn:'A condition chooses whether an instruction runs. currentScore > 0 is true only for positive scores. Indent the selected updates inside the if block. A function gives instructions a name so the same method can process different lists.',example:positiveExample,extraExamples:[{title:'Read a simple function',code:functionExample}],starter:'def positiveScores(scoreList):\n    # Print the positive count, then positive total.\n    pass\n\nscores = [5, 0, -2, 7]\npositiveScores(scores)\n',check:'w2-positive',goal:'Define positiveScores(scoreList). Use a loop and condition; print positive count, then positive total.',steps:['Read the condition and function examples.','Build your function; ignore zero and negative values.','Test it with two different lists.'],hints:['Initialise count and total inside the function, before its loop.','Only update them inside if currentScore > 0:'],solution:'def positiveScores(scoreList):\n    scoreCount = 0\n    totalScore = 0\n\n    for currentScore in scoreList:\n        if currentScore > 0:\n            scoreCount = scoreCount + 1\n            totalScore = totalScore + currentScore\n\n    print(scoreCount)\n    print(totalScore)\n\nscores = [5, 0, -2, 7]\npositiveScores(scores)\n',teacher:'The parameter scoreList holds the argument supplied by the call. Count and total are local to each call and reset before its loop. Only 5 and 7 satisfy > 0, giving 2 and 12. >= 0 would incorrectly count zero.',tests:'[5, 0, -2, 7] -> 2 then 12; [-3, 0] -> 0 then 0; [2, 2] -> 2 then 4.',error:'Place the final prints after the loop but still inside the function.'}),
  card({id:'gold2',level:'Gold',colour:'gold',phase:'Open extension',time:'Your pace',title:'One pass, three results',intro:'Find the count, total and maximum of a non-empty list.',learn:'The maximum is the largest value seen so far. Start it at the first item, scores[0], so an all-negative list works. Compare each score with the current maximum and replace it when larger. Count and total can update during the same loop.',example:maximumExample,starter:'scores = [-5, -2, -9]\n\n# Use one for loop for count, total and maximum.\n# Print count, then total, then maximum.\n',check:'w2-maximum',goal:'For a non-empty scores list, use one for loop. Print count, total and maximum on separate lines.',steps:['Build all three results in one loop.','Test an all-negative list and a one-item list.','Explain why you start maximum at scores[0].'],hints:['Initialise the count and total at zero. Initialise maximumScore from scores[0].','Use if currentScore > maximumScore: inside the loop.'],solution:'scores = [-5, -2, -9]\nscoreCount = 0\ntotalScore = 0\nmaximumScore = scores[0]\n\nfor currentScore in scores:\n    scoreCount = scoreCount + 1\n    totalScore = totalScore + currentScore\n    if currentScore > maximumScore:\n        maximumScore = currentScore\n\nprint(scoreCount)\nprint(totalScore)\nprint(maximumScore)\n',teacher:'The maximum begins at -5, changes to -2, then stays -2. Starting at zero would invent a value not in this list. A single loop visits n items; three separate scans would visit them three times. Both grow linearly with n; one pass does less traversal.',tests:'[-5, -2, -9] -> 3 / -16 / -2; [4] -> 1 / 4 / 4; [0, 0] -> 2 / 0 / 0. Empty lists are outside this task contract.',error:'scores[0] needs a non-empty list. State that assumption instead of claiming the method handles an empty list.'}),
];
export const allTasks = [...lesson, ...extensions];
export const coreChecks = lesson.filter(t => t.check).length;
export const meta = {number:2,title:'Lists, loops & running totals',date:'15 September 2026'};
