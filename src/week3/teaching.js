// Student-facing teaching sits beside the stable task/check IDs so existing work survives revisions.
const block=(title,...paragraphs)=>({title,paragraphs});
const table=(caption,headers,rows)=>({caption,headers,rows});
const check=(question,answer)=>({question,answer});
const manualGuide=(noun='fence pieces')=>({
 start:'Work this out by hand first. You will type a number, not Python code.',
 reading:[
  block('What are we trying to build?',`Imagine a field drawn as a rectangle of cells. We want one complete line of ${noun} from the top to the bottom. We may choose any column. We do not need to fill every gap in the whole field.`, `A # means a piece is already in place. A . means an empty space. Each empty space in our chosen column needs one new piece. Existing pieces cost us nothing.`),
  block('How do I read the input?','In the example below, 2 3 means 2 rows and 3 columns. Rows run across →. Columns run down ↓. The next two lines draw the field. The numbers at the top describe its size; they are not cells to count.'),
  block('How do I choose?','Count the dots in the first column. Write that count down. Do the same for every other column. Choose the smallest count. Minimum means smallest. An integer is a whole number such as 0, 1 or 2.')
 ],
 worked:table('Follow the example: count down each column',['Column','Cells, top to bottom','New pieces needed'],[['1','. then .','2'],['2','# then .','1'],['3','. then .','2']]),
 walkthrough:['Column 2 already has one piece. Only its bottom gap needs filling.','The smallest count is 1, so the answer file contains 1. The column number is 2, but that is not the answer requested.'],
 question:check('Do I add the three column counts together?','No. We need to complete only one column. Adding would count work in several columns. Choose the smallest count instead.'),
 inputHelp:'The first line gives rows then columns. The lines below are your new field. Use the clickable drawing to follow one column at a time; selecting a column does not calculate its answer.',
 steps:['Read the field below. Point to a row across and a column down.','Select column 1. Count its dots from top to bottom and write the count on paper. Repeat for every column.','Circle the smallest of your counts. Type that whole number in Your answer.','Choose Check my answer. If it needs another look, recount one column at a time.','Choose Download answer file. Open the downloaded .txt file: it should contain only your number.'],
 success:'You have checked one whole-number answer, saved its file and can explain which gaps need filling. No program is required for this task.'
});
export const teaching={
 a1:manualGuide(),
 a2:{...manualGuide('lights'),reading:[
  block('The same method in a new story','A theatre has vertical strips of lights. A # is a light that works; a . is a missing light. We want to finish just one strip from top to bottom, using as few new lights as possible. The layout works like the field in the last question.'),
  block('What if two strips are equally good?','A tie means two choices need the same number. You may choose either strip. The answer still gives the number of new lights for one strip. Do not add the two tied counts.'),
  block('Read the small example first','2 3 means two rows of three lights. Count down each of its three strips. The table below shows why the middle strip needs the fewest new lights.')
 ],question:check('Two strips each need two lights. Do I write 2 or 4?','Write 2. You are completing one strip, so you need only two new lights.')},
 a3:{...manualGuide(),reading:[
  block('Can the answer be zero?','Yes. If a column contains only # symbols, it already reaches from top to bottom. It has no gaps, so we do not need to add anything.'),
  block('Always check the smallest possible case','Zero is a valid whole-number answer. Do not add a piece just because a question asks how many new pieces are needed. First inspect the field. A complete column means the work is already done.')
 ],worked:table('A different small field: #. / #.',['Column','Cells, top to bottom','New pieces needed'],[['1','# then #','0'],['2','. then .','2']]),walkthrough:['The first column is already complete. Its gap count is zero.','The second column needs two pieces, but we can choose the first. The minimum is 0.'],question:check('Does a zero answer mean the program or calculation failed?','No. It can mean the chosen column was already complete. Explain which cells show that no work is needed.')},
 retrieval:{
 start:'Start with a familiar loop. You can run the supplied code without writing anything yet.',
 reading:[block('A list lets us keep several values','In cells = [".", "#"], the square brackets hold a list with two items. Quotation marks tell Python these symbols are text. The dot and hash are items in the list; the commas separate them.'),block('A loop visits one item at a time','for cell in cells: means “take each item from cells and call the current item cell”. The indented print line runs once for each visit. After the loop, Python carries on with the next line that is not indented.'),block('A counter remembers how many visits we have made','The starter below adds 1 on every visit. It counts all cells, whether they contain a dot or a hash. Later we will change the rule so only dots count.')],
 worked:table('What the small example does',['Visit','Value of cell','Action'],[['1','.','Print .'],['2','#','Print #']]),
 walkthrough:['Each symbol appears on its own line because print runs twice.','In your starter, cellCount begins at 0. After three visits its value is 3.'],question:check('Are the quotation marks part of each cell?','No. They mark text in Python code. The stored value is the symbol inside them.'),
 inputHelp:'All data is already in the cells list. Leave Program input empty.',
 steps:['Choose Run under Your Python code. Find 3 in Program output.','Change the # inside the list to a dot. Run again: there are still three items.','Add another item inside the list, using a comma and quotation marks. Run again: there are now four visits.','In Explain your work, describe the difference between changing an item and adding an item.'],success:'You have run the program and explained why the number of items controls this counter.'
 },
 'condition-example':{
 start:'Read how a condition works, then run the complete example. You do not need to fill any gaps here.',
 reading:[block('We only want to count gaps','The old counter adds 1 for every cell. A fence is already built, so it should not increase the number of new pieces. We need a choice inside the loop: add 1 for a dot; do nothing for a hash.'),block('Two equals signs ask a question','cell == "." asks “is this cell a dot?” The answer is True or False. The word if tells Python to run the indented block only when the answer is True. One equals sign, as in gapCount = 0, stores a value instead.'),block('Spaces show which instructions belong together','The if line sits four spaces inside the for loop. The update sits another four spaces inside the if. print(gapCount) starts at the left edge, so it runs once after all cells have been checked.')],
 worked:table('Follow the counter for #, ., #',['Cell','Is it a dot?','gapCount after this visit'],[['#','False — skip the update','0'],['.','True — add 1','1'],['#','False — keep the counter','1']]),
 walkthrough:['Start at 0 before looking at any cell. A hash keeps the value; it does not reset it.','Only one visit adds 1. The final print shows the number of gaps, not the number of visits.'],question:check('Why is print outside the loop?','We want one final answer after all visits. Putting print inside the loop would show several running counts.'),
 inputHelp:'This example uses the cells list in the code. Program input can stay empty.',
 steps:['Choose Run to try the complete program.','Replace one # in cells with a dot. Run again and compare the two outputs.','For a slower view, open Editor tools & walkthrough, choose Walk through, then use Next line.','Explain one visit where the counter changes and one where it stays the same.'],success:'You can point to the comparison and explain when the indented update runs.'
 },
 b1:{
 start:'Your first coding change: replace pass with a condition and a counter update.',
 reading:[block('Turn the counting rule into code','columnCells already contains the cells from one vertical column, ordered from top to bottom. Do not count across a grid yet. We need to visit each value and add 1 only when it is a dot.'),block('Read the working example below','cells is the list. gapCount is the counter. for gives cell the next symbol. if checks that symbol. gapCount = gapCount + 1 means “take the old count, add one, and store the new count”. It is an update, not a maths equation.'),block('What am I meant to change?','Your starter has a comment beginning with # and a line saying pass. A comment is a note for the reader. pass tells Python to do nothing; it is a placeholder. Delete pass and put your if block there. Keep the supplied list, the starting counter and the final print.')],
 worked:table('Why the example prints 1',['Visit','cell','Counter before → after'],[['1','#','0 → 0'],['2','.','0 → 1'],['3','#','1 → 1']]),walkthrough:['The if line has four leading spaces because it belongs to the loop.','Its update has eight leading spaces because it belongs to the if. The final print has none.'],question:check('Should I set gapCount to 0 every time I see a hash?','No. A hash adds nothing. Resetting would erase gaps counted earlier. Start at 0 once, before the loop.'),
 inputHelp:'Use columnCells in the code. No typed input is needed. Check my code will try other lists too.',
 steps:['Read the example and its counter table. You can explore it in a separate workspace.','Return to Your Python code. Replace pass with an if line that compares cell with a dot.','Inside that if, add 1 to gapCount. Check the spaces at the start of both lines.','Choose Run. The supplied four cells contain three dots, so your output should be 3.','Choose Check my code. Explain why a list of only hashes should print 0.'],success:'Your code counts dots in fresh lists and prints one final count.'
 },
 b2:{
 start:'Use your dot-counting rule again. This time, pick one cell from each row first.',
 reading:[block('A grid is a list of rows','grid = [".#", ".."] stores two rows. Each row is a string: a line of text. Python can choose one character from a string using square brackets.'),block('Python counts positions from zero','For the row ".#", position 0 holds . and position 1 holds #. The position is called an index. columnIndex = 1 means the second column, not the first.'),block('How do rows help us travel down a column?','The loop gives currentRow the next whole row. currentRow[columnIndex] chooses one cell from that row. Keep columnIndex the same as you visit all the rows: you collect the cells down one column. In the example we print those cells; in your task you count their dots.')],
 worked:table('Example: keep columnIndex at 1',['Row visited','Positions 0, 1','Cell chosen at position 1'],[['.#','. , #','#'],['..','. , .','.']]),walkthrough:['The loop prints # and then .: these are the second-column cells from top to bottom.','To count instead of print, compare that selected cell with a dot and update a counter.'],question:check('Is grid[1] the second column?','No. grid[1] is the second whole row. Choose currentRow[1] on each row to move down the second column.'),
 inputHelp:'grid and columnIndex are supplied in your code. Leave Program input empty. Tests change both values.',
 steps:['In the example workspace, change columnIndex to 0 and Run to inspect the first column. Then return to your code.','In your starter, replace pass with a condition using currentRow[columnIndex].','Increase gapCount inside that condition only for a dot.','Run with the supplied grid and index. Then try columnIndex = 0: the first column has three gaps.','Choose Check my code and explain the difference between a position and a gap count.'],success:'Your counter follows whichever column index is supplied, including index 0.'
 },
 b3:{
 start:'This is a supported stretch step. The input reader is supplied; work on the two marked gaps below it.',
 reading:[block('From one column to every column','In the last task we chose one column. Now we repeat that method for each possible column and remember the smallest count. This needs two loops, one inside the other. Such loops are called nested loops.'),block('Separate the two jobs','The outer loop chooses a column index. range(columnCount) supplies 0, 1, 2, and so on, stopping before columnCount. For each chosen column, the inner loop visits the rows and counts its dots. Reset gapCount to 0 before counting the next column.'),block('Remember the best answer so far','bestCount keeps the smallest count seen. If a new gapCount is smaller, replace bestCount. Otherwise keep the old best. We start bestCount at rowCount because no column can have more gaps than it has rows.'),block('First practise choosing the smallest number','The small example below gives you the column costs already calculated. Watch how bestCount remembers 3, then 1, then keeps 1. Your task will calculate each cost using the column counter before comparing it.')],
 worked:table('Follow the smaller-number example',['Cost being checked','Comparison','Best after the check'],[['3','3 < 3 is False','3'],['1','1 < 3 is True','1'],['2','2 < 1 is False','1']]),walkthrough:['The best value changes only when a smaller cost is found.','In your grid solver, count one whole column first. Compare after the row loop, while still inside the column loop.'],question:check('Which value resets for each column: gapCount or bestCount?','gapCount resets because we are starting a new column. bestCount must remember the earlier columns.'),
 inputHelp:'Program input starts with rows and columns, followed by one line for each row. The supplied reader turns this into rowCount, columnCount and grid. Leave that reader unchanged in this task.',
 steps:['Leave the input-reading lines at the top as they are. Find the comment Count the gaps in this column.','Replace its pass with your condition and counter update from the previous task.','At Keep the smaller count, add a comparison and an update to bestCount. Align the comparison with the inner for line, not with its body.','Run with the sample in Program input. Then use Check my code for other shapes and an already complete column.','Explain why one counter resets and the other does not.'],success:'The program prints the smallest column gap count for different fields. If this is new, finish with teacher support and revisit it later.'
 },
 b4:{
 start:'The solver is complete. Your job is only to read the input it will use.',
 reading:[block('Where does competition data come from?','A general program cannot keep using the same list typed into its code. input() reads the next line from Program input. Each call moves to a new line. Here the first line contains the size and the following lines contain the field.'),block('Read the size in two steps','For a line such as 2 4, input().split() makes the list ["2", "4"]. These are still text. int converts each piece to a whole number. Piece 0 is the number of rows; piece 1 is the number of columns.'),block('Collect the rows','Start grid = [] to make an empty list. Repeat rowCount times: read one line and append it to grid. append means add one item at the end. A row such as .#.. stays as text; do not convert its symbols to numbers.'),block('Keep input and output separate','Type data in Program input, not underneath the Python code. Do not use a prompt inside input or print labels such as Answer:. A competition checker expects only the required answer.')],
 worked:table('Read the example header 2 4',['Expression','Result','Meaning'],[['input().split()','["2", "4"]','Two pieces of text'],['int(header[0])','2','Two rows'],['int(header[1])','4','Four columns']]),walkthrough:['The example prints the two converted sizes to show what was read. Your final solver should print only its answer.','The extra example below reads two rows into a list. In your solution, replace 2 with rowCount.'],snippets:[{title:'A smaller example: collect two rows',code:'grid = []\nfor rowNumber in range(2):\n    currentRow = input()\n    grid.append(currentRow)',note:'With input rows .# and .., grid becomes [".#", ".."]. This snippet collects data; it does not print yet.'}],question:check('The header says 3 2. How many row lines should follow?','Three lines, each containing two symbols. The first number controls how many lines the reader needs.'),
 inputHelp:'For this task, keep the existing solver below your new reader. It requires the exact variable names rowCount, columnCount and grid.',
 steps:['At the top of the starter, read and split the first input line.','Create rowCount and columnCount by converting the two pieces to integers.','Create an empty grid list. Use a loop to read and append rowCount lines.','Run the supplied sample. Then try input 1 1 followed by # on the next line; it should need no new fence.','Choose Check my code. If you see EOFError, compare the first number with how many row lines you supplied.'],success:'Your program reads different field sizes without changing the code.'
 },
 b5:{
 start:'This program contains one deliberate mistake. Run it unchanged first so you can see the problem.',
 reading:[block('A bug is a mistake in a program','A program can finish without an error message and still give a wrong answer. Testing means checking whether the output matches the problem, not just whether Python runs.'),block('Choose a test whose answer you know','A field with two rows of only dots needs two new pieces in every column. Its minimum must be 2. If the program prints 0, this test has exposed a mistake.'),block('Look at the starting value','The program starts bestCount at 0. It replaces that value only when a gap count is smaller. Gap counts cannot be negative, so none can beat 0. A starting value must let a real answer replace it. The example below starts with a real list value; for a grid, the number of rows is a safe starting limit.')],
 worked:table('Two ways to begin, using values 5 and 3',['Starting smallest','After comparing 5','After comparing 3'],[['5 — a real value','5','3 — correct'],['0 — an invented minimum','0','0 — wrong']]),walkthrough:['The faulty version can accidentally pass a test that really needs zero pieces. One passing case is not enough.','Change the starting value, not the final print. Printing 2 would patch only one input.'],question:check('Why test an all-dot field as well as an all-hash field?','An all-hash field should print 0, so the faulty program seems correct there. An all-dot field needs a positive answer and reveals the fault.'),
 inputHelp:'The all-dot input is already supplied. Run it before changing the program, then run the same input after your repair.',
 steps:['Run the starter unchanged. Compare the printed value with the expected 2.','Find bestCount = 0. Replace its starting value with the number of rows.','Run the same input again. It should now print 2.','Choose Check my code to check cases that need different answers.','In Explain your work, record the failing input, the old answer and why your change fixes it.'],success:'You have evidence of a failing test before the repair and a correct answer after it.'
 },
 pb1:{start:'Use the conditional counter from Main Task 2 with a line of typed input.',reading:[block('A string can be visited like a list','column = input() stores one line of text. A for loop can visit each character in that text, from left to right. Treat the line as the cells in one column: each dot needs a new piece.'),block('Keep the rule the same','Start at zero before the loop. Add one when the current character is a dot. A hash leaves the count unchanged. Print once after the loop.')],worked:table('Another input: .##.',['Character','Counter afterwards'],[['.','1'],['#','1'],['#','1'],['.','2']]),walkthrough:['The text contains four characters, but only two gaps.','The Python example below uses a list. Your program reads a string; the same loop-and-condition idea works for both.'],question:check('Should I include quotation marks in Program input?','No. Type just dots and hashes. Quotation marks are needed when writing text inside Python code.'),inputHelp:'One line of dots and hashes. For example, .##. represents four cells.',steps:['Replace pass with a dot comparison and counter update.','Run the sample. Then replace Program input with #### and run again.','Choose Check my code and explain why fences do not increase the count.'],success:'Your counter handles both a line containing gaps and a line with no gaps.'},
 pb2:{start:'Practise the running total from Week 2. No typed input is needed.',reading:[block('Count sticks, not groups','stickCounts holds the stock of each colour. If counts are [1, 3], we have one stick of one colour and three of another. That is two groups but four sticks.'),block('Add the value you visit','totalSticks begins at 0. Each loop visit adds currentCount, the size of the current group. Adding 1 would count groups instead of sticks.')],worked:table('Follow the example counts [1, 3]',['Group size','Total before','Total after'],[['1','0','1'],['3','1','4']]),walkthrough:['The update uses the item value, not a fixed 1.','Print after all groups have been added. This task counts materials; it does not arrange the fan.'],question:check('For a single group containing 5 sticks, is the total 1 or 5?','5. There is one group, but the question asks for the number of sticks.'),inputHelp:'The list is already in the code. Checks replace stickCounts with fresh lists.',steps:['Replace pass with an update that adds currentCount to totalSticks.','Run with the supplied list.','Choose Check my code. Explain why you added the group size.'],success:'Your total includes every stick, even when group sizes differ.'},
 br1:{start:'Combine counting with a decision: do we have enough pieces?',reading:[block('There are two jobs','First count the gaps in the column. Then compare this count with the number of available pieces. Do not decide before the counting loop finishes.'),block('Enough includes exactly enough','The symbol <= means “less than or equal to”. If we need 3 pieces and have 3, the answer is YES. else gives the instruction to run when the if question is False.')],worked:table('Try three budgets for a column with 3 gaps',['Available','Is 3 <= available?','Answer'],[['2','False','NO'],['3','True','YES'],['4','True','YES']]),walkthrough:['The example code uses ENOUGH to explain the decision.','Your task requires the exact words YES and NO in capital letters.'],question:check('Why would using < give a wrong answer when stock exactly matches the gaps?','3 < 3 is False. We need 3 <= 3, which is True, because three pieces are enough for three gaps.'),inputHelp:'Line 1: a whole number of pieces. Line 2: the column symbols. Do not put them on the same line.',steps:['Write a loop that counts dots in column.','After the loop, compare gapCount with pieceCount using <=.','Print YES for enough pieces and NO otherwise.','Run and Check my code. Try exactly enough stock as one of your own tests.'],success:'Your decision works for too few, exactly enough and more than enough pieces.'},
 br2:{start:'The gap counts are already calculated. Choose the smallest one.',reading:[block('Keep a best-so-far value','Imagine comparing repair quotes. Hold onto the cheapest quote seen. A later quote replaces it only if it is cheaper. The same method works for a list of column costs.'),block('Start with a real cost','repairCosts[0] selects the first list value. Start there, then visit every cost. If cost < bestCost, store cost as the new best. An equal cost can leave the best unchanged.')],worked:table('Example values [5, 3, 4]',['Cost','Best before','Best after'],[['5','5','5'],['3','5','3'],['4','3','3']]),walkthrough:['The last value is not necessarily the smallest. Keep the earlier 3 when 4 is visited.','Zero is only a valid minimum if the list actually contains it.'],question:check('Do two equal cheapest costs need to be added?','No. The question asks for one smallest cost, not the total cost of both repairs.'),inputHelp:'Use the repairCosts list already in the code. Program input stays empty.',steps:['Add a for loop between the starting value and final print.','Compare each cost with bestCost and replace bestCost only when smaller.','Run, then Check my code. Try a list where the cheapest value appears first.'],success:'The answer stays correct wherever the smallest value appears in the list.'},
 br3:{start:'Inspect an existing fan design. You do not need to rearrange its colours.',reading:[block('Neighbours sit directly beside each other','In RGR, the two red sticks are separated by green, so the design is allowed. In RRG, the first two sticks touch and share a colour, so the design is not allowed.'),block('Remember the previous colour','previousColour begins as empty text because there is no earlier stick on the first visit. Compare each colour with the previous one, then store the current colour ready for the next visit.'),block('Remember any failure','True and False are Python values for yes/no decisions. valid starts as True. Set it to False when a pair clashes. Never set it back to True just because a later pair is different.')],worked:table('Follow the example BRRG',['Previous','Current','Same neighbours?'],[['empty','B','No previous stick'],['B','R','No'],['R','R','Yes — remember failure'],['R','G','No, but the earlier failure remains']]),walkthrough:['The code example prints pairs to show what is compared. Its first line starts with a space because previous is empty.','Your final program should print a single YES or NO after inspecting the entire design.'],question:check('Does RGR fail because R appears twice?','No. Only adjacent colours must differ. Repeating a colour with another colour between is allowed.'),inputHelp:'One line of uppercase R, G and B, without spaces.',steps:['Loop over colours. Compare each colour with previousColour.','If they match, set valid to False. Update previousColour on every visit.','After the loop, use valid to print YES or NO.','Run and check. Include a clash near the beginning to test whether you remember it.'],success:'An early clashing pair is still reported even if later pairs are different.'},
 br4:{start:'Count the colours in six selected candies, then check both totals.',reading:[block('What makes this split fair?','One child needs three candies of one colour and the other needs three of the other colour. We are allowed to separate the candies by colour, so their order in the input does not matter.'),block('Use two counters','For each R, add one to redCount. For each B, add one to blueCount. The input contains only these two letters. At the end, use == to compare a count with 3. and joins two conditions that must both be True.')],worked:table('Counting the small example RBRB',['Candy','Red total','Blue total'],[['R','1','0'],['B','1','1'],['R','2','1'],['B','2','2']]),walkthrough:['The small Python example demonstrates the red counter only. It is not a full six-candy check.','For six candies, three of each colour is valid. Four of one and two of the other is not.'],question:check('Must the input be RRRBBB in that exact order?','No. Any order with three R and three B works because we can group the candies afterwards.'),inputHelp:'One line containing exactly six uppercase R/B letters, with no spaces.',steps:['Add a loop that updates the correct counter for each colour.','After counting, check whether both totals equal 3.','Print YES or NO. Run two different orders with the same counts.','Choose Check my code.'],success:'Your answer depends on colour totals, not their order.'},
 si1:{start:'Bring the main-task methods together with less starter support. You may revisit B2–B4.',reading:[block('Write a plan before writing code','Read the field. For each column, count its gaps. Remember the smallest count. Print that count. These are four separate jobs; build and test one at a time.'),block('Keep track of what each variable means','rowCount and columnCount describe the shape. columnIndex chooses a column. gapCount counts only that column. bestCount remembers the smallest completed count. Resetting bestCount each column loses earlier results.'),block('Read the limits','N is the number of rows; M is the number of columns. Each is between 1 and 20 in this classroom task. That means even a one-cell field is allowed. A rectangular field need not have the same number of rows and columns.')],worked:table('From field .#. / ... to a minimum',['Column','Dot count','Best so far'],[['1','2','2'],['2','1','1'],['3','2','1']]),walkthrough:['The worked field has two rows and three columns. The middle column is cheapest.','The Python example below starts with costs already counted. Your program also needs to calculate them.'],question:check('What if the field has only one column?','Count its gaps and print that count. There are no other columns to compare, but the same loops still work.'),inputHelp:'Line 1: N M separated by a space. Then exactly N lines, each with M dots/hashes.',steps:['Write the four-job plan on paper.','Write the input reader, then a counter for one column.','Repeat the counter across all columns and keep the minimum.','Run a rectangular field, a single column and an already complete field. Then check your code.'],success:'Your complete program works with different shapes without hard-coded rows or answers.'},
 si2:{start:'Build a few alternating rows with coloured counters before writing the calculation.',reading:[block('What does alternating mean?','An alternating row changes colour at every step: R B R B R. Two equal neighbours, such as R R, are not allowed. We want the longest row we can make, and may leave unused sticks aside.'),block('The scarcer colour limits the pairs','With four red and two blue sticks, make two R B pairs. That uses two of each colour. One extra red can go on an end, giving R B R B R. A second extra red would touch another red.'),block('Handle equal stock separately','If both colours have the same stock, use all the sticks as pairs. If one colour has more, use twice the smaller stock plus one. min(redCount, blueCount) gives the smaller stock.')],worked:table('Try small stocks',['Red, blue','One longest row','Length'],[['2, 2','R B R B','4'],['4, 2','R B R B R','5'],['1, 4','B R B','3']]),walkthrough:['The Python example prints the number of pairs, not the final row length.','Your task prints only the length. You do not need to print the arrangement itself.'],question:check('Why not always add the two stock counts?','Some sticks may have to stay unused. Extra sticks of the common colour would create equal neighbours.'),inputHelp:'One line containing the red count then the blue count, separated by a space.',steps:['Draw a row for equal counts and another for unequal counts.','Use if to separate these cases. Calculate the maximum length in each.','Print one length. Run and Check my code.'],success:'Your calculation works when either colour is scarcer and when their stocks are equal.'},
 si3:{start:'Compare bags of letters. Ignore their positions; count how many of each letter you have.',reading:[block('Rearranging is free; replacing costs one','abc and bca have the same letters, so no replacement is needed. An anagram is a rearrangement of the same letters. For this task, the first word must end up with the same letter counts as the second.'),block('Find spare letters','If the first word has two a letters and the second needs only one, one a is spare: it is a surplus. Replace that spare a with a letter we are missing. One replacement fixes both the extra letter and a shortage.'),block('Count only one side of each replacement','word.count(letter) counts how often a letter occurs. For a, b and c, add the positive surplus in the first word. Do not also add the shortages, or you will count each replacement twice.')],worked:table('Worked pair: aac → abb',['Letter','Count in aac','Count needed for abb','Surplus'],[['a','2','1','1'],['b','0','2','0 — these are missing'],['c','1','0','1']]),walkthrough:['Replace one a with b, then replace c with b. Two changes make abb.','The positive surpluses are 1 + 1 = 2. Their sum is the required replacement count.'],question:check('Why does abc → bca need zero replacements?','The counts match: one a, one b and one c in each word. We may rearrange the first word for free.'),inputHelp:'Two lines, one word on each. The words have the same length and contain only lowercase a, b and c.',steps:['Loop over the text "abc" to visit the three possible letters.','For each letter, count its occurrences in both words.','If the first count is larger, add the difference to replacements.','Print once. Run an anagram case, then Check my code.'],success:'Anagrams need zero changes and each spare letter is counted only once.'},
 go1:{start:'Optional stretch: use numbered paper packs first. This task needs nested loops and a dictionary.',reading:[block('Understand one selection before counting all of them','Each input line describes a pack of two candies. Pick exactly three different packs. Their six candies must contain exactly two colours, with three of each. Colour numbers are labels, not quantities to add.'),block('Same contents, different packs','Two packs can both contain colours 1 and 2, but they remain separate packs. Picking pack 1 is a different choice from picking pack 2. However, picking packs 1, 2, 3 in a different order is not a new selection.'),block('Choose in increasing order','Python indexes begin at 0. Make the second index larger than the first and the third larger than the second. This stops the loops choosing a pack twice or counting the same three packs in different orders. The example below demonstrates this rule for pairs first.'),block('A dictionary is a tally chart','frequencies = {} starts an empty tally. A key identifies a colour; its value counts candies of that colour. Before adding to a colour for the first time, create its count with 0. Then increase it by 1. len(frequencies) tells us how many different colours appeared. frequencies.values() gives their counts.'),block('Check and count a whole selection','Join the three pack lists with +, count the six colours, then require exactly two colours and both counts to be 3. Add 1 to validCount for that entire selection. For fewer than three packs there is no possible selection, so print 0.')],worked:table('One valid selection of three packs',['Chosen pack','Candies inside','Running tally'],[['A','1, 1','colour 1: 2'],['B','1, 2','colour 1: 3; colour 2: 1'],['C','2, 2','colour 1: 3; colour 2: 3']]),walkthrough:['The selection contributes 1 to validCount, not 6: we are counting selections, not candies.','The extra example below makes one tally entry. In your solution, start a fresh dictionary for each selection, then repeat the check and update for its six candies.'],snippets:[{title:'A smaller example: tally one candy',code:'frequencies = {}\ncolour = 2\nif colour not in frequencies:\n    frequencies[colour] = 0\nfrequencies[colour] = frequencies[colour] + 1',note:'The dictionary is now {2: 1}: colour 2 has appeared once. The colour label is the key; 1 is its count.'}],question:check('Would counts 2, 2, 2 pass because there are six candies?','No. There are three colours, but the rule requires exactly two colours with three candies of each.'),inputHelp:'First line: number of packs N. Each of the next N lines gives two colour labels separated by a space. The starter reads these into packs. Our N ≤ 20 limit makes trying every triple practical.',steps:['Keep the supplied input reader. On paper, test one valid and one invalid selection.','Create validCount = 0. Add three loops whose indexes strictly increase.','Inside the third loop, join the three selected packs and build a fresh colour tally.','If there are exactly two colours with three candies each, add one to validCount.','Print validCount after all loops. Run and check, including fewer than three packs.'],success:'Each set of three distinct packs is inspected once, and only valid selections increase the answer.'},
 exit:{start:'Use the method you have practised on a fresh field. Try independently first.',inputHelp:'Read the dimensions, then the field. Each dot is a gap; each hash is an existing fence. You need to complete only one vertical column.',steps:['Count down each column by hand, or use your own Python method.','Type the smallest gap count and check your answer.','Download the answer file. In Explain your work, say why your number counts new pieces, and record any help you used.','Choose My learning report and save your PDF and backup.'],success:'You have an answer, an explanation of the method, and saved evidence to share with your teacher.'}
};
